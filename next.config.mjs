/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['nextui.org'],
    },
    reactStrictMode: false // so annoying to render twice the three.js canvas !!!!
};

export default nextConfig;
